package com.qiwang.ins_android.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Instagram 风格颜色定义。
 *
 * 采用极致黑白化配色方案，高对比度、扁平化视觉效果。
 * 不使用 Material Design 3 的动态色彩（Dynamic Color）。
 */

// 主色调
val InstagramBlue = Color(0xFF0095F6)  // Instagram 经典蓝，用于关注按钮和链接

// 背景与表面
val BackgroundWhite = Color(0xFFFFFFFF)  // 纯白背景

// 文字颜色
val TextPrimary = Color(0xFF262626)      // 深灰黑，用于正文
val TextSecondary = Color(0xFF8E8E8E)    // 中灰色，用于辅助信息、时间戳

// 分割线与边框
val OutlineGray = Color(0xFFDBDBDB)      // 极浅灰色，用于细分割线

// 输入框背景
val InputBackground = Color(0xFFEFEFEF)  // 浅灰色，用于搜索框、输入框背景
